name: flagsmith_exmaple_app
description: A new Flutter project.
publish_to: "none" # Remove this line if you wish to publish to pub.dev
version: 1.0.1+1

environment:
  sdk: ">=2.13.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flagsmith: 
    path: ../
  flagsmith_storage_sharedpreferences: ^2.1.0+1
    
  collection: ^1.15.0
  equatable: ^2.0.3
  flutter_bloc: ^7.2.0
  flutter_svg: ^0.22.0
  get_it: ^7.2.0
  google_fonts: ^2.1.0
  hexcolor: ^2.0.5
  rxdart: ">=0.23.1 <0.27.5"
dev_dependencies:
  flutter_lints: ^1.0.4
  flutter_test:
    sdk: flutter
  
  test: ^1.16.0
flutter:
  uses-material-design: true
  assets:
    - res/
